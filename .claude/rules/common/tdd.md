---
paths:
  - "**/*.test.ts"
  - "**/*.test.tsx"
  - "e2e/**/*.spec.ts"
---
# TDD Policy

When editing or creating test code, you MUST use the `/tdd` skill and follow its workflow.
Skipping steps or writing implementation before a failing test is prohibited.

Do NOT rely on your own judgment for TDD process — always load and follow the skill.
